# A-LangGraph-based-Multi-Agent-Movie-Recommendation-System
Built on LangGraph, this multi-agent platform takes user natural-language preferences, retrieves candidates via 3 parallel semantic searches, deduplicates and fuses results, then uses an LLM to rerank and generate Chinese justifications.
